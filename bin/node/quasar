#!/bin/sh

trap_ctrlc() {
    docker compose stop quasar_dev;
    docker compose rm --force quasar_dev;
    exit 2;
}

trap "trap_ctrlc" 2;

docker compose up -d quasar_dev;
docker compose logs -f quasar_dev